<div class="page-content" style="padding: 8rem 4rem; max-width: 900px; margin: 0 auto;">
    <h1 class="section-title" style="color: var(--charcoal); margin-bottom: 2rem;">{{ page.title }}</h1>
    <div class="page-body" style="font-size: 1.1rem; line-height: 1.8; color: var(--slate);">
        {{ page.content }}
    </div>
</div>
